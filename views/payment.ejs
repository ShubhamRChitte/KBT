<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment - KBT Cafe</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/css/kbtcafe.css" />
    
    <!-- Google fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Forum&family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet"> 
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Montserrat', sans-serif;
        }
        
        body {
            background-color: #1a1a1a;
            color: #f4ebd0;
            background-image: url('/images/payment-bg.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-blend-mode: overlay;
        }
        
        .payment-container {
            max-width: 900px;
            margin: 2.5rem auto;
            padding: 2.5rem;
            background-color: rgba(30, 30, 30, 0.9);
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(5px);
            border: 1px solid rgba(218, 165, 32, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        
        .payment-container:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(218, 165, 32, 0.2);
        }
        
        .payment-header {
            text-align: center;
            margin-bottom: 2.5rem;
            color: #f4ebd0;
            border-bottom: 2px solid rgba(218, 165, 32, 0.3);
            padding-bottom: 1.5rem;
        }
        
        .payment-header h1 {
            font-size: 2.2rem;
            color: goldenrod;
            margin-bottom: 0.8rem;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
            letter-spacing: 1px;
        }
        
        .payment-header p {
            color: #d4c9a8;
            font-size: 1.1rem;
        }
        
        .payment-amount {
            text-align: center;
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 2.5rem;
            color: #f4ebd0;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
            background: linear-gradient(90deg, #d4af37, #f4cd6d, #d4af37);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: shine 2s infinite linear;
        }
        
        @keyframes shine {
            0% {
                background-position: 0% center;
            }
            50% {
                background-position: 100% center;
            }
            100% {
                background-position: 0% center;
            }
        }
        
        .payment-methods {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            gap: 1.5rem;
        }
        
        .payment-method {
            flex: 1;
            min-width: 320px;
            padding: 2rem;
            background-color: rgba(40, 40, 40, 0.9);
            border: 1px solid rgba(218, 165, 32, 0.3);
            border-radius: 12px;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }
        
        .payment-method::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, transparent, goldenrod, transparent);
            transform: scaleX(0);
            transition: transform 0.5s ease;
        }
        
        .payment-method:hover {
            border-color: goldenrod;
            box-shadow: 0 0 20px rgba(218, 165, 32, 0.3);
            transform: translateY(-5px);
        }
        
        .payment-method:hover::before {
            transform: scaleX(1);
        }
        
        .payment-method h3 {
            font-size: 1.3rem;
            margin-bottom: 1.5rem;
            color: #f4ebd0;
            display: flex;
            align-items: center;
            border-bottom: 1px solid rgba(218, 165, 32, 0.2);
            padding-bottom: 0.8rem;
        }
        
        .payment-method h3 i {
            margin-right: 0.8rem;
            color: goldenrod;
            font-size: 1.5rem;
        }
        
        .qr-section {
            text-align: center;
            margin-top: 1rem;
        }
        
        .qr-section p {
            color: #d4c9a8;
            margin-bottom: 1rem;
        }
        
        .qr-code {
            width: 220px;
            height: 220px;
            margin: 1.5rem auto;
            border: 2px solid goldenrod;
            padding: 0.5rem;
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        
        .qr-code:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 25px rgba(218, 165, 32, 0.4);
        }
        
        .qr-code img {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }
        
        .payment-form {
            margin-top: 1.5rem;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 0.8rem;
            font-weight: 500;
            color: #f4ebd0;
            font-size: 1.05rem;
        }
        
        .form-control {
            width: 100%;
            padding: 0.9rem 1rem;
            background-color: rgba(30, 30, 30, 0.8);
            border: 1px solid rgba(218, 165, 32, 0.3);
            border-radius: 8px;
            font-size: 1rem;
            color: #f4ebd0;
            transition: all 0.3s ease;
        }
        
        .form-control:focus {
            outline: none;
            border-color: goldenrod;
            box-shadow: 0 0 10px rgba(218, 165, 32, 0.2);
        }
        
        .btn {
            background: linear-gradient(to right, #c19a49, goldenrod, #c19a49);
            color: #1a1a1a;
            border: none;
            padding: 1.2rem 1.5rem;
            font-size: 1.1rem;
            border-radius: 8px;
            cursor: pointer;
            width: 100%;
            font-weight: 600;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: 0.5s;
        }
        
        .btn:hover {
            background: linear-gradient(to right, #d4af37, #f4cd6d, #d4af37);
            box-shadow: 0 5px 15px rgba(218, 165, 32, 0.4);
            transform: translateY(-3px);
        }
        
        .btn:hover::before {
            left: 100%;
        }
        
        .payment-footer {
            text-align: center;
            margin-top: 2.5rem;
            color: #d4c9a8;
            font-size: 1rem;
            padding-top: 1.5rem;
            border-top: 1px solid rgba(218, 165, 32, 0.2);
        }
        
        .payment-steps {
            margin: 2rem 0;
            padding: 1.5rem;
            background-color: rgba(40, 40, 40, 0.7);
            border-radius: 10px;
            border-left: 3px solid goldenrod;
        }
        
        .payment-steps h3 {
            margin-bottom: 1.2rem;
            color: #f4ebd0;
            font-size: 1.2rem;
        }
        
        .payment-steps ol {
            padding-left: 1.8rem;
        }
        
        .payment-steps li {
            margin-bottom: 0.8rem;
            color: #d4c9a8;
            line-height: 1.5;
        }
        
        .payment-steps li::marker {
            color: goldenrod;
        }
        
        .upload-preview {
            width: 100%;
            max-height: 200px;
            margin-top: 1rem;
            border-radius: 8px;
            display: none;
            object-fit: contain;
            border: 1px solid goldenrod;
        }
        
        @media (max-width: 768px) {
            .payment-methods {
                flex-direction: column;
            }
            
            .payment-container {
                margin: 1.5rem;
                padding: 1.5rem;
            }
            
            .payment-amount {
                font-size: 2.5rem;
            }
        }
    </style>
</head>
<body>
    <%- include("includes/navbar") %>
    
    <div class="payment-container">
        <div class="payment-header">
            <h1>Complete Your Payment</h1>
            <p>Order #<%= orderNumber %> • <%= date %></p>
        </div>
        
        <div class="payment-amount">
            ₹<%= grandTotal %>.00
        </div>
        
        <div class="payment-methods">
            <div class="payment-method">
                <h3><i class="fas fa-qrcode"></i> Scan QR Code</h3>
                
                <div class="qr-section">
                    <p>Scan this QR code with your UPI app</p>
                    <div class="qr-code">
                        <img src="/images/kbt-payment-qr.png" alt="Payment QR Code">
                    </div>
                    <p>or pay to UPI ID: <strong>kbtcafe@ybl</strong></p>
                </div>
                
                <div class="payment-steps">
                    <h3>How to pay:</h3>
                    <ol>
                        <li>Open any UPI app (GPay, PhonePe, Paytm, etc.)</li>
                        <li>Scan the QR code or enter UPI ID</li>
                        <li>Enter amount: ₹<%= grandTotal %>.00</li>
                        <li>Complete payment in your app</li>
                        <li>Upload screenshot below</li>
                    </ol>
                </div>
            </div>
            
            <div class="payment-method">
                <h3><i class="fas fa-receipt"></i> Confirm Payment</h3>
                
                <form action="/kbt/payment/confirm" method="POST" enctype="multipart/form-data" class="payment-form">
                    <input type="hidden" name="orderAmount" value="<%= grandTotal %>">
                    <input type="hidden" name="orderNumber" value="<%= orderNumber %>">
                    
                    <div class="form-group">
                        <label for="paymentMethod">Payment Method</label>
                        <select id="paymentMethod" name="paymentMethod" class="form-control" required>
                            <option value="">Select Payment Method</option>
                            <option value="upi">UPI (GPay/PhonePe/Paytm)</option>
                            <option value="card">Credit/Debit Card</option>
                            <option value="cash">Cash on Delivery</option>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <label for="paymentId">Transaction/Reference ID (optional)</label>
                        <input type="text" id="paymentId" name="paymentId" class="form-control" placeholder="Enter transaction ID if available">
                    </div>
                    
                    <div class="form-group">
                        <label for="paymentProof">Upload Payment Screenshot</label>
                        <input type="file" id="paymentProof" name="paymentProof" class="form-control" accept="image/*" onchange="previewImage(this)">
                        <img id="preview" class="upload-preview">
                    </div>
                    
                    <button type="submit" class="btn">Confirm Payment</button>
                </form>
            </div>
        </div>
        
        <div class="payment-footer">
            <p>Having trouble? Contact our support at support@kbtcafe.com</p>
        </div>
    </div>
    
    <%- include("includes/footer") %>
    
    <script>
        function previewImage(input) {
            const preview = document.getElementById('preview');
            if (input.files && input.files[0]) {
                const reader = new FileReader();
                
                reader.onload = function(e) {
                    preview.src = e.target.result;
                    preview.style.display = 'block';
                }
                
                reader.readAsDataURL(input.files[0]);
            }
        }
    </script>
</body>
</html> 