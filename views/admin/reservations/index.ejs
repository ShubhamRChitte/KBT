<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Reservations - KBT Cafe</title>
    <link rel="stylesheet" href="/css/kbtcafe.css">
    <style>
        .admin-container {
            max-width: 1200px;
            margin: 120px auto 50px;
            padding: 30px;
        }
        
        .admin-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }
        
        .btn-primary {
            display: inline-block;
            padding: 10px 20px;
            background-color: var(--gold-crayola);
            color: black;
            border-radius: 5px;
            font-weight: bold;
            transition: opacity 0.3s;
            border: none;
            cursor: pointer;
        }
        
        .btn-primary:hover {
            opacity: 0.9;
        }
        
        .btn-danger {
            background-color: #ff6b6b;
            color: white;
        }
        
        .reservations-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            background-color: var(--eerie-black-2);
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
        }
        
        .reservations-table th, 
        .reservations-table td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid var(--eerie-black-4);
        }
        
        .reservations-table thead th {
            background-color: var(--eerie-black-3);
            color: var(--gold-crayola);
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .reservations-table tbody tr:hover {
            background-color: var(--eerie-black-3);
        }
        
        .actions {
            display: flex;
            gap: 10px;
        }
        
        .alert {
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 5px;
        }
        
        .alert-danger {
            background-color: #ff6b6b;
            color: white;
        }
        
        .alert-success {
            background-color: #51cf66;
            color: white;
        }
        
        .empty-state {
            text-align: center;
            padding: 50px;
            background-color: var(--eerie-black-2);
            border-radius: 10px;
            margin-top: 20px;
        }
        
        .empty-state h2 {
            color: var(--gold-crayola);
            margin-bottom: 15px;
        }
        
        .empty-state p {
            color: var(--quick-silver);
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <%- include("../../includes/navbar") %>
    
    <div class="admin-container">
        <div class="admin-header">
            <h1 class="headline-1">Manage Reservations</h1>
            <a href="/kbt/admin" class="btn-primary">Back to Dashboard</a>
        </div>
        
        <% if(locals.error && error.length > 0) { %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
        <% } %>
        
        <% if(locals.success && success.length > 0) { %>
            <div class="alert alert-success">
                <%= success %>
            </div>
        <% } %>
        
        <% if(reservations && reservations.length > 0) { %>
            <table class="reservations-table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Phone</th>
                        <th>People</th>
                        <th>Date</th>
                        <th>Time</th>
                        <th>Message</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% reservations.forEach(reservation => { %>
                        <tr>
                            <td><%= reservation.name %></td>
                            <td><%= reservation.phone %></td>
                            <td><%= reservation.person %></td>
                            <td><%= new Date(reservation.date).toLocaleDateString() %></td>
                            <td><%= reservation.time %></td>
                            <td><%= reservation.message %></td>
                            <td class="actions">
                                <a href="/kbt/admin/reservations/<%= reservation._id %>/edit" class="btn-primary">Edit</a>
                                <form action="/kbt/admin/reservations/<%= reservation._id %>?_method=DELETE" method="POST" style="display: inline;">
                                    <button type="submit" class="btn-primary btn-danger" onclick="return confirm('Are you sure you want to delete this reservation?')">Delete</button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <div class="empty-state">
                <h2>No Reservations Found</h2>
                <p>There are currently no table reservations.</p>
            </div>
        <% } %>
    </div>
    
    <script src="/js/kbtcafe.js"></script>
</body>
</html> 