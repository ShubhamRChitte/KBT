<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Menu Item - KBT Cafe</title>
    <link rel="stylesheet" href="/css/kbtcafe.css">
    <style>
        .admin-container {
            max-width: 800px;
            margin: 120px auto 50px;
            padding: 30px;
        }
        
        .admin-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }
        
        .btn-primary {
            display: inline-block;
            padding: 10px 20px;
            background-color: var(--gold-crayola);
            color: black;
            border-radius: 5px;
            font-weight: bold;
            transition: opacity 0.3s;
            border: none;
            cursor: pointer;
        }
        
        .btn-primary:hover {
            opacity: 0.9;
        }
        
        .form-container {
            background-color: var(--eerie-black-2);
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: var(--gold-crayola);
        }
        
        .form-control {
            width: 100%;
            padding: 12px;
            background-color: var(--eerie-black-4);
            color: white;
            border: 1px solid var(--davys-grey);
            border-radius: 5px;
        }
        
        .alert {
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 5px;
        }
        
        .alert-danger {
            background-color: #ff6b6b;
            color: white;
        }
        
        .alert-success {
            background-color: #51cf66;
            color: white;
        }
    </style>
</head>
<body>
    <%- include("../../includes/navbar") %>
    
    <div class="admin-container">
        <div class="admin-header">
            <h1 class="headline-1">Add New Menu Item</h1>
            <a href="/kbt/admin/menu" class="btn-primary">Back to Menu</a>
        </div>
        
        <% if(locals.error && error.length > 0) { %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
        <% } %>
        
        <% if(locals.success && success.length > 0) { %>
            <div class="alert alert-success">
                <%= success %>
            </div>
        <% } %>
        
        <div class="form-container">
            <form action="/kbt/admin/menu" method="POST">
                <div class="form-group">
                    <label for="title">Item Name</label>
                    <input type="text" id="title" name="title" class="form-control" required>
                </div>
                
                <div class="form-group">
                    <label for="description">Description</label>
                    <textarea id="description" name="description" class="form-control" rows="4" required></textarea>
                </div>
                
                <div class="form-group">
                    <label for="price">Price (₹)</label>
                    <input type="number" id="price" name="price" class="form-control" min="1" step="any" required>
                </div>
                
                <div class="form-group">
                    <label for="image">Image URL</label>
                    <input type="text" id="image" name="image" class="form-control" placeholder="URL to image">
                    <small style="color: var(--quick-silver); display: block; margin-top: 5px;">Leave empty for default image</small>
                </div>
                
                <button type="submit" class="btn-primary" style="width: 100%; margin-top: 10px;">Add Menu Item</button>
            </form>
        </div>
    </div>
    
    <script src="/js/kbtcafe.js"></script>
</body>
</html> 