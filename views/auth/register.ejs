<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - KBT Cafe</title>
    <link rel="stylesheet" href="/css/kbtcafe.css">
    <style>
        .login-container {
            background-color: var(--eerie-black-2);
            max-width: 500px;
            margin: 120px auto 50px;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-control {
            width: 100%;
            padding: 12px;
            background-color: var(--eerie-black-4);
            color: white;
            border: 1px solid var(--davys-grey);
            border-radius: 5px;
            margin-top: 5px;
        }
        
        .auth-btn {
            background-color: var(--gold-crayola);
            color: black;
            font-weight: bold;
            padding: 12px 20px;
            width: 100%;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
        }
        
        .auth-btn:hover {
            opacity: 0.9;
        }
        
        .alert {
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 5px;
        }
        
        .alert-danger {
            background-color: #ff6b6b;
            color: white;
        }
        
        .alert-success {
            background-color: #51cf66;
            color: white;
        }
        
        .login-link {
            text-align: center;
            margin-top: 20px;
            color: var(--quick-silver);
        }
        
        .login-link a {
            color: var(--gold-crayola);
        }
        
        /* Responsive improvements */
        @media screen and (max-width: 768px) {
            .login-container {
                margin-top: 100px;
                width: 90%;
                padding: 25px;
            }
        }
        
        @media screen and (max-width: 480px) {
            .login-container {
                margin-top: 90px;
                width: 95%;
                padding: 20px 15px;
            }
            
            .form-control {
                padding: 10px;
            }
            
            .auth-btn {
                padding: 10px;
            }
            
            h2.headline-2 {
                font-size: 1.8rem;
            }
            
            .form-group {
                margin-bottom: 15px;
            }
        }
    </style>
</head>
<body>
    <%- include("../includes/navbar") %>
    
    <div class="login-container">
        <h2 class="headline-2 text-center">Register</h2>
        
        <% if(locals.error && error.length > 0) { %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
        <% } %>
        
        <% if(locals.success && success.length > 0) { %>
            <div class="alert alert-success">
                <%= success %>
            </div>
        <% } %>
        
        <form action="/kbt/auth/register" method="POST">
            <div class="form-group">
                <label for="username">Username</label>
                <input type="text" id="username" name="username" class="form-control" required autocomplete="username">
            </div>
            
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" class="form-control" required autocomplete="email">
            </div>
            
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" class="form-control" required minlength="6" autocomplete="new-password">
                <small style="color: var(--quick-silver); display: block; margin-top: 5px;">Password must be at least 6 characters long</small>
            </div>
            
            <button type="submit" class="auth-btn">Register</button>
        </form>
        
        <div class="login-link">
            Already have an account? <a href="/kbt/auth/login">Login here</a>
        </div>
    </div>
    
    <script src="/js/kbtcafe.js"></script>
</body>
</html> 